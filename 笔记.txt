role属性的作用:本质上是增强语义性，当现有的HTML标签不能充分表达语义性的时候，就可以借助role来说明。
通常这种情况出现在一些自定义的组件上（例如一个tree组件，html里目前还没有tree这个标签），
这样可增强组件的可访问性、可用性和可交互性。 


模态框（Modal）的层最好是body的直接子元素，触发元素可以包含在其他元素中



col-md-push-1
col-md-pull-2








